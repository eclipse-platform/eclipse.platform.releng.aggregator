
Leaving (near) empty file here so directory gets committed. 

This folder is used to hold "data" awaiting a cronjob to collet results from Hudson tests, 
and initiating the "summarizing" scripts and updload to "downloads". 

It is best if this directory created by committer ID. 
 